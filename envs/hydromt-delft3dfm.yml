---
# note that these are the developer dependencies
name: hydromt-delft3dfm

channels:
  - conda-forge

dependencies:
  - black # linting
  - cartopy # to run examples
  - flit>=3.4
  - jupyter # to run examples
  - matplotlib # to run examples
  - hydromt>= 0.9.0
  - nbsphinx # docs notebook examples
  - numpy
  - pandas
  - pip
  - pre-commit # linting
  - pydata-sphinx-theme # docs
  - pyproj
  - pytest # tests
  - pytest-cov # tests
  - pytest-mock # tests
  - pytest-timeout # test
  - responses # tests
  - ruff # linting
  - shapely>=2.0.0
  - sphinx # docs
  - sphinx-design # docs
  - xarray
  #- xugrid>=0.6.5 # TODO: currently requires branch below
  - networkx
  - llvmlite>=0.39.1 # hydrolib-core
  - pip:
      #- hydrolib-core==0.5.2 # TODO: currently requires branch below
      - git+https://github.com/deltares/hydrolib-core@569-bumping-meshkernel-version-to-latest-v300
      - git+https://github.com/deltares/xugrid@169-update-meshkernel-api-calls-after-new-release
      - meshkernel>=3.0.0
