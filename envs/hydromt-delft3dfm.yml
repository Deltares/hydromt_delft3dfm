---
# note that these are the developer dependencies
name: hydromt-delft3dfm

channels:
  - conda-forge

dependencies:
  - black # linting
  - cartopy # to run examples
  - flit>=3.4
  - geopandas>=0.10
  - jupyter # to run examples
  - matplotlib # to run examples
  - hydromt>=0.10.0,<1.0.0a0 # To avoid getting v1 alpha
  - nbsphinx # docs notebook examples
  - numpy
  - pandas>=2.0.0
  - pip
  - pre-commit # linting
  - pydata-sphinx-theme # docs
  - pyproj
  - pytest # tests
  - pytest-cov # tests
  - pytest-mock # tests
  - pytest-timeout # test
  - python>=3.9
  - responses # tests
  - ruff # linting
  - shapely>=2.0.0
  - sphinx # docs
  - sphinx-design # docs
  - xarray
  - xugrid>=0.9.0
  - networkx
  - llvmlite>=0.39.1 # hydrolib-core
  - pip:
      - hydrolib-core>=0.8.0
      - meshkernel>=4.3.0
