---
# note that these are the developer dependencies
name: hydromt-delft3dfm

channels:
  - conda-forge

dependencies:
  - flit>=3.4
  - geopandas
  - hydromt>= 0.9.0
  - numpy
  - pandas
  - pip
  - pyflwdir >=0.5.4
  - pyproj
  - python=3.9.*
  - scipy
  - shapely>=2.0.0
  - xarray
  - xugrid>=0.6.5 # TODO: will require new xugrid release that includes xugrid branch 169-update-meshkernel-api-calls-after-new-release
  - networkx
  - llvmlite>=0.39.1 # hydrolib-core
  - pip:
      - hydrolib-core==0.5.2 # TODO: currently requires branch 569-bumping-meshkernel-version-to-latest-v300
      - meshkernel>=3.0.0
